/**
 * Teacher onboarding page: launches the guided setup flow for subject and class
 * assignment before a teacher can access the dashboard.
 */

import TeacherOnboarding from "../../../components/auth/TeacherOnboarding";

const TeacherOnboardingPage = () => {
  return <TeacherOnboarding />;
};

export default TeacherOnboardingPage;
